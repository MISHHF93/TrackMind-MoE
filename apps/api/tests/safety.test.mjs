import test from 'node:test';import assert from 'node:assert/strict';import { ApprovalStore, ResponsibleAIGovernor } from '../dist/index.js';
for (const action of ['official-results','scratch-horse','medication-decision','clear-vet-flag','emergency-action','payout','race-start','race-stop','disciplinary-decision']) test(`blocks ${action} without approval`,()=>{const g=new ResponsibleAIGovernor(new ApprovalStore());assert.equal(g.assertMayAutomate(action,'rec-1').allowed,false);});
test('allows protected action with human approval',()=>{const s=new ApprovalStore();s.saveApproval({id:'ap-1',recommendationId:'rec-1',action:'race-start',status:'approved',approver:'steward-1',timestamp:new Date().toISOString(),reason:'reviewed',evidence:['camera-1']});const g=new ResponsibleAIGovernor(s);assert.equal(g.assertMayAutomate('race-start','rec-1').allowed,true);});
