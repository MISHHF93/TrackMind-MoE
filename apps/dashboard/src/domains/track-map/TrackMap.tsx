import type { TrackMapDto } from '../../types.js';
export function TrackMap({ map }: { map: TrackMapDto }) { return <section aria-label="Track map"><h2>Track Map {map.mock && '(mock)'}</h2><p>Race distance: {map.distanceMeters}m · Starting gate: {map.startingGate.sectorId} at {map.startingGate.metersFromStart}m</p><div>{map.sectors.map((s)=><article key={s.id}><h3>{s.name}</h3><p>{s.startMeters}-{s.endMeters}m · {s.condition}</p><ul>{map.measurements.filter(m=>m.sectorId===s.id).map(m=><li key={m.sectorId}>Moisture {m.moisture}% · Compaction {m.compaction}</li>)}{map.assets.filter(a=>a.sectorId===s.id).map(a=><li key={a.id}>{a.label} ({a.type}) · {a.status}</li>)}</ul></article>)}</div></section>; }
