services:
  postgres:
    image: postgres:16
    environment: { POSTGRES_PASSWORD: trackmind, POSTGRES_USER: trackmind, POSTGRES_DB: trackmind }
    ports: ["5432:5432"]
    volumes: ["../../db/migrations:/docker-entrypoint-initdb.d:ro"]
