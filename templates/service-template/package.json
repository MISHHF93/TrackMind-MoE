{
  "name": "@trackmind/service-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Contract-only TrackMind bounded-context service scaffold.",
  "scripts": {
    "test": "node --test tests/**/*.test.mjs"
  }
}
